[project]
name = "uuidgen"
version = "0.1.0"
description = "uuidgen by mimifuwacc"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
uuidgen = "uuidgen.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"